import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luciad.mydbtester',
  appName: 'mydbtester',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
